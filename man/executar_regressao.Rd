% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regressao.R
\name{executar_regressao}
\alias{executar_regressao}
\title{Executa uma especificação de regressão}
\usage{
executar_regressao(con, espec, dados = NULL)
}
\arguments{
\item{con}{Conexão criada por \code{\link[=conecta]{conecta()}}.}

\item{espec}{Objeto \code{eduBR_espec} criado por \code{\link[=especificar_regressao]{especificar_regressao()}} ou
\code{\link[=ler_espec]{ler_espec()}}.}

\item{dados}{Fonte de dados opcional (\code{tbl} ou objeto eduBR). Tem
precedência sobre \code{espec$fonte}.}
}
\value{
Um \code{tibble} de classe \code{eduBR_regressoes}, com as colunas de corte,
\code{n}, \code{modelo}, \code{coeficientes}, \code{metricas} e \code{predicoes} (list-columns;
achate com \code{\link[=coeficientes]{coeficientes()}}, \code{\link[=metricas]{metricas()}} ou \code{tidyr::unnest()}).
}
\description{
Ajusta o modelo descrito em \code{espec} para cada combinação das colunas de
corte (\code{espec$cuts}). Os dados vêm do argumento \code{dados} (um \code{tbl} lazy ou
um objeto eduBR) ou do domínio \code{espec$fonte}. O modelo é ajustado com o
\code{parsnip} e os resultados são devolvidos como list-columns padronizadas.
}
\examples{
\dontrun{
con <- conecta()
espec <- especificar_regressao(
  outcome = "ideb_observado",
  predictors = c("nota_media"),
  cuts = c("ano", "sg_uf", "etapa"),
  fonte = "ideb"
)
executar_regressao(con, espec)
}

}
