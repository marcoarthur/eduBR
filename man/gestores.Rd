% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gestor.R
\name{gestores}
\alias{gestores}
\title{Gestores escolares por rede e território}
\usage{
gestores(
  con,
  rede = NULL,
  uf = NULL,
  regiao = NULL,
  localizacao = NULL,
  ano = 2025
)
}
\arguments{
\item{con}{Conexão criada por \code{\link[=conecta]{conecta()}}.}

\item{rede}{Filtro pelas redes. Aceita códigos (1–4), nomes
(\code{"Federal"}, \code{"Estadual"}, \code{"Municipal"}, \code{"Privada"}) ou
\code{"publica"}/\code{"privada"}.}

\item{uf}{Filtro opcional pela sigla da UF (ex.: \code{"SP"}).}

\item{regiao}{Filtro opcional pela macrorregião (nome ou sigla).}

\item{localizacao}{Filtro opcional pela localização (\code{1} urbana, \code{2} rural,
ou \code{"urbana"}/\code{"rural"}).}

\item{ano}{Ano do censo (padrão \code{2025}, o único disponível).}
}
\value{
Objeto S3 de classe \code{eduBR_gestores} (consulta preguiçosa).
}
\description{
Cruza \code{clean.censo_gestor} com \code{clean.censo_escolas} por
\verb{(nu_ano_censo, co_entidade)}, anexando a rede (\code{rede}), a categoria da
escola privada (\code{categoria_privada}), a localização e a macrorregião
derivada da UF. Mantém apenas as colunas de contagem (\verb{qt_gest_bas_*})
relevantes para o perfil. A consulta é preguiçosa; materialize com
\code{\link[tibble:as_tibble]{as_tibble()}} ou passe o objeto a \code{\link[=perfil_gestor]{perfil_gestor()}}.
}
\examples{
\dontrun{
con <- conecta()
gestores(con)                      # todas as escolas, 2025
gestores(con, rede = "Municipal")  # só municipais
gestores(con, regiao = "Nordeste")
}

}
