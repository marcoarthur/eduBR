% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gestor.R
\name{perfil_gestor}
\alias{perfil_gestor}
\title{Perfil modal dos gestores escolares}
\usage{
perfil_gestor(
  dados,
  corte = "rede",
  unidade = c("gestor", "escola"),
  dimensoes = NULL
)
}
\arguments{
\item{dados}{Objeto \code{eduBR_gestores} (de \code{\link[=gestores]{gestores()}}) ou um \code{data.frame}
com as colunas de contagem e as chaves de corte.}

\item{corte}{Agrupamento do perfil: \code{"brasil"}, \code{"rede"} (padrão),
\code{"regiao"}, \code{"uf"} ou \code{"categoria_privada"}.}

\item{unidade}{Unidade de agregação: \code{"gestor"} ou \code{"escola"}.}

\item{dimensoes}{Dimensões incluídas. \code{NULL} (padrão) usa todas.}
}
\value{
Objeto S3 de classe \code{eduBR_perfil_gestor} com \verb{$proporcoes}
(longo: \code{corte}, \code{dimensao}, \code{categoria}, \code{n}, \code{denom}, \code{prop},
\code{composicao}), \verb{$modal} (categoria modal, \code{prop_modal} e concentração —
índice de Herfindahl — por corte × dimensão) e \verb{$n} (escolas e gestores
por corte).
}
\description{
Agrega as contagens de \code{\link[=gestores]{gestores()}} em cortes e deriva, para cada dimensão,
a distribuição de categorias e a categoria \strong{modal} (a mais frequente).
}
\details{
Duas unidades de agregação:
\itemize{
\item \code{"gestor"} (padrão): cada gestor pesa 1 (soma das contagens por escola);
\item \code{"escola"}: cada escola contribui com \strong{uma} observação na categoria
predominante daquele colégio (sensibilidade ao peso de escolas pequenas).
}

Denominadores por dimensão (base válida): dimensões com \code{"soma"} usam a
soma das categorias; \code{"complemento"} usa o total de gestores com o
fechamento como categoria; cor/raça "não declarada" sai do denominador e
aparece com \code{composicao = FALSE}; vínculo vale apenas para escolas
públicas.
}
\examples{
\dontrun{
con <- conecta()
p <- perfil_gestor(gestores(con), corte = "rede")
p$modal
}

}
