% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desempenho.R
\name{classificar_desempenho}
\alias{classificar_desempenho}
\title{Classifica o desempenho escolar em alto/médio/baixo}
\usage{
classificar_desempenho(
  dados,
  nota = "nota_media",
  grupo = "etapa",
  cortes = NULL,
  rotulos = c("baixo", "medio", "alto")
)
}
\arguments{
\item{dados}{\code{data.frame} com a coluna de nota.}

\item{nota}{Nome da coluna de nota usada como base.}

\item{grupo}{Nome da coluna usada para agrupar o cálculo dos terços.
Se ausente (ou \code{NULL}), os terços são globais a \code{dados}.}

\item{cortes}{Opcional. Pode ser \code{NULL} (terços por grupo), um vetor
numérico com os dois pontos de corte (aplicado a todos os grupos) ou uma
lista nomeada \code{grupo = c(corte1, corte2)}.}

\item{rotulos}{Rótulos dos três níveis, do menor para o maior.}
}
\value{
Um \code{tibble} com a coluna adicional \code{nivel} (fator ordenado). Os
cortes usados ficam no atributo \code{limites_desempenho}; acesse com
\code{\link[=limites_desempenho]{limites_desempenho()}}.
}
\description{
Cria o alvo de classificação a partir de uma coluna de nota (padrão
\code{nota_media}, média SAEB de matemática e português). Por padrão os cortes
são os \strong{terços globais} da nota dentro de cada grupo (etapa); cortes
alternativos podem ser passados em \code{cortes}.
}
\examples{
\dontrun{
con <- conecta()
d <- classificar_desempenho(features_escola(con))
limites_desempenho(d)
}

}
