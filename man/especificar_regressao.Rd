% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/espec.R
\name{especificar_regressao}
\alias{especificar_regressao}
\title{Especificação de regressão}
\usage{
especificar_regressao(
  outcome,
  predictors,
  cuts = NULL,
  modelo = c("linear", "logistico"),
  fonte = NULL,
  filtro = NULL,
  id = NULL
)
}
\arguments{
\item{outcome}{Nome da coluna do desfecho (string).}

\item{predictors}{Vetor de nomes das colunas preditoras.}

\item{cuts}{Vetor opcional de colunas de recorte; a regressão é ajustada
separadamente para cada combinação.}

\item{modelo}{\code{"linear"} (padrão) ou \code{"logistico"}.}

\item{fonte}{Nome de um domínio do catálogo (\code{\link[=catalogo]{catalogo()}}) usado como base,
quando \code{dados} não é informado em \code{\link[=executar_regressao]{executar_regressao()}}.}

\item{filtro}{Lista nomeada opcional de igualdades aplicadas antes do
ajuste (ex.: \code{list(ano = 2023, sg_uf = "SP")}).}

\item{id}{Identificador opcional da especificação.}
}
\value{
Objeto de classe \code{eduBR_espec}.
}
\description{
Cria um objeto \code{eduBR_espec} descrevendo uma regressão a ser executada por
\code{\link[=executar_regressao]{executar_regressao()}}: desfecho, preditores, cortes (recortes), modelo e
fonte de dados. O mesmo objeto pode ser rodado para muitas combinações de
cortes.
}
\examples{
especificar_regressao(
  outcome = "ideb_observado",
  predictors = c("nota_media"),
  cuts = c("ano", "sg_uf", "etapa"),
  fonte = "ideb"
)

}
