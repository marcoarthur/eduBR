% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coletar.R
\name{coletar}
\alias{coletar}
\title{Coleta um objeto eduBR ou uma consulta lazy}
\usage{
coletar(x, n = NULL, avisar = TRUE)
}
\arguments{
\item{x}{Objeto eduBR ou \code{tbl} lazy.}

\item{n}{Número máximo de linhas (opcional). Sem \code{n}, materializa tudo.}

\item{avisar}{Emitir aviso quando \code{n} é \code{NULL} e a consulta é remota?
Padrão \code{TRUE}.}
}
\value{
Um \code{tibble}.
}
\description{
Materializa um objeto eduBR ou um \code{tbl} lazy (dbplyr) como \code{tibble}. Com
\code{n}, aplica um \code{LIMIT} (via \code{head()}) antes de coletar; sem \code{n}, emite um
aviso de custo quando a consulta é remota.
}
\examples{
\dontrun{
con <- conecta()
coletar(escolas(con, uf = "SP"), n = 100)
coletar(escolas(con, uf = "SP"))            # aviso de custo
}

}
