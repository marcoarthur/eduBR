% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desempenho.R
\name{features_escola}
\alias{features_escola}
\title{Features escolares para modelagem de desempenho}
\usage{
features_escola(
  con,
  etapa = c("fundamental_i", "fundamental_ii"),
  publica = TRUE
)
}
\arguments{
\item{con}{Conexão criada por \code{\link[=conecta]{conecta()}}.}

\item{etapa}{Etapas incluídas; por padrão \code{fundamental_i} e
\code{fundamental_ii}. Use \code{NULL} para todas (inclui \code{ensino_medio}).}

\item{publica}{Mantém apenas escolas públicas (\code{tp_dependencia} 1–3)?
Padrão \code{TRUE}.}
}
\value{
Objeto S3 de classe \code{eduBR_features} (consulta preguiçosa).
}
\description{
Le a MV \code{analytics.escola_features} (materialização de
\code{analytics.prepare_school_data()}, com censo 2025 e IDEB/INSE 2023), que
reúne, por escola e etapa, infraestrutura, matrículas, docentes, gestão,
INSE e razões derivadas, junto da nota média SAEB (\code{nota_media}, média de
matemática e português). A consulta é preguiçosa (filtros aplicados antes
do \code{collect}); use \code{\link[=classificar_desempenho]{classificar_desempenho()}} para criar o alvo.
}
\examples{
\dontrun{
con <- conecta()
counts <- features_escola(con, etapa = "fundamental_i")
}

}
