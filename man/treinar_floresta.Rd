% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/floresta.R
\name{treinar_floresta}
\alias{treinar_floresta}
\title{Treina a floresta aleatoria}
\usage{
treinar_floresta(
  dados,
  alvo = "nivel",
  features = NULL,
  semente = 2023,
  trees = 500,
  min_node_size = 5,
  ...
)
}
\arguments{
\item{dados}{\code{data.frame} com o alvo e as features.}

\item{alvo}{Nome da coluna alvo (fator). Padrao \code{nivel}.}

\item{features}{Vetor de colunas usadas como preditoras. Padrao \code{NULL}:
todas exceto \code{alvo} e os identificadores/medidas de desempenho
(\code{co_entidade}, \code{id_escola}, \code{etapa}, \code{nota_matematica}, \code{nota_portugues},
\code{nota_media}).}

\item{semente}{Semente aleatoria.}

\item{trees}{Numero de arvores.}

\item{min_node_size}{Tamanho minimo do no folha.}

\item{...}{Argumentos adicionais passados a \code{\link[ranger:ranger]{ranger::ranger()}}.}
}
\value{
Objeto de classe \code{eduBR_floresta}: lista com \code{modelo} (saida do
ranger), \code{alvo}, \code{features}, \code{formula} e \code{classes}.
}
\description{
Ajusta uma Random Forest de \strong{classificacao com probabilidade} (\code{ranger})
para um alvo categorico (ex.: \code{nivel} criado por \code{\link[=classificar_desempenho]{classificar_desempenho()}}).
A importancia de cada variavel e calculada por \strong{permutacao}, permitindo a
reducao de dimensao via \code{\link[=importancia_floresta]{importancia_floresta()}}.
}
\examples{
\dontrun{
d <- classificar_desempenho(features_escola(con))
partes <- dividir_dados(d)
rf <- treinar_floresta(partes$treino)
importancia_floresta(rf)
}

}
