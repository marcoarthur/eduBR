% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conexao.R
\name{conecta}
\alias{conecta}
\title{Abre uma conexão com a base do EduMaps}
\usage{
conecta(service = "edumaps", ...)
}
\arguments{
\item{service}{Nome do serviço no \verb{~/.pg_service.conf}. Por padrão,
\code{"edumaps"}.}

\item{...}{Argumentos adicionais repassados a \code{\link[DBI:dbConnect]{DBI::dbConnect()}}.}
}
\value{
Um objeto \code{DBIConnection} (S4 do \code{RPostgres}), pronto para uso com
\code{DBI}, \code{dplyr}/\code{dbplyr} e demais pacotes compatíveis.
}
\description{
Cria uma conexão PostgreSQL usando o driver \code{RPostgres} a partir de um
\emph{service} do libpq (arquivo \verb{~/.pg_service.conf}). O uso de \code{service}
mantém host, usuário e senha fora do código e permite alternar entre
ambientes — por exemplo, \code{"edumaps"} (desenvolvimento) ou
\code{"edumaps_local"} (o mesmo serviço visto de dentro da rede dos containers).
}
\examples{
\dontrun{
con <- conecta()
DBI::dbListTables(con)
DBI::dbDisconnect(con)
}

}
